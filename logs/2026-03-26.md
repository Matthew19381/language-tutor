# Log sesji — 2026-03-26
**Projekt:** Language Tutor | **Sesja:** Nowa (start projektu w systemie)

## Zmiany
- `audio_service.py`: Retry 3x z exponential backoff (0.5s/1s/2s) — fix 403 edge-tts
- `achievement_service.py`: ACHIEVEMENT_DEFS przetłumaczone na polski
- `lesson_generator.py`: Prompt tipów — CRITICAL: pisz w native_language
- `routers/lessons.py`: concept-flashcards — czytelny błąd gdy AI zwraca 0 konceptów
- `DailyLesson.jsx`: renderMarkdown() obsługuje #/##/### i **bold** w gramatyce
- `DailyLesson.jsx`: Dialog — układ lewa/prawa wg pola `speaker` (nie indeksu)
- `placement.py`: needs_placement = was_new (rejestruj PRZED zapisem profilu)
- `Stats.jsx`: handleChangeLanguage — brak 1.5s delay, wyczyść lesson cache, reload natychmiast
- `Stats.jsx`: TodayCompletion — reaktywny useState + window.focus refresh
- `DailyTest.jsx`: Cache pytań testu w localStorage — test nie regeneruje się przy wejściu

## Stan
- Działa: audio (retry), osiągnięcia PL, markdown gramatyki, dialog układ, tipy PL, zmiana języka, wskaźnik ukończenia, test bez regeneracji
- Nienaprawione P0: LESSON-1 (regeneracja lekcji), LESSON-2 (zły dzień startu), UI-1 (reszta angielskich elementów), AUDIO-2 (brak audio w wielu miejscach)

## Decyzje
- ADR-001: Stack audio — edge-tts z retry (nie zmiana na gTTS)

## Wnioski
- edge-tts 403 jest intermittent (race condition z Bing) — retry wystarczy, nie potrzeba zmiany provider
- needs_placement miał buga: profil zapisywany PRZED sprawdzeniem → zawsze wysyłał na placement dla A1
- Test bez submisji nie zostawia śladu w DB → cache w localStorage to właściwe rozwiązanie
